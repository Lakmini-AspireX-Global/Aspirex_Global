from odoo import api, fields, models, _


class ManufacturerManufacturer(models.Model):
    _name = 'manufacturer.manufacturer'
    _inherit = ['mail.thread', 'mail.activity.mixin']
    _description = 'Manufacturer part number for the product'

    name = fields.Char(string='Manufacturer', required=True)
    #colour = fields.Integer(string='Colour')